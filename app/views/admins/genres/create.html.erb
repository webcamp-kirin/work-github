<h1>ジャンル一覧・追加</h1>

<%= form_with model:@genres,url: admins_genres_path, method: :post, local:true do |f| %>
<table>
  <thead>
    <tr>
  　 <th>ジャンル名</th>
  　  <th></th>
     <th><%= f.text_field :name %></th>
     <th></th>
     <th><%= f.submit '新規登録' %></th>
    </tr>
  </thead>
</table> 


<% end %>